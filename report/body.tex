% !TEX root = 米粒粘连导致漏数的问题与解决.tex
\section{引言}

本次作业要求数出照片里有多少粒米，并解决连体米粒计数不准的问题。

恰好我大二打过电赛，物联网设计竞赛和数学建模，负责问题分析，视觉循迹以及目标识别。于是我先利用已有的知识做了初步尝试。

最初按最基本的思路做了一遍。先转灰度，再用 Otsu 法\cite{otsu1979}自动选一个阈值把米粒和背景分开，最后统计连通域的个数。连通域指二值图中连在一起的一片前景像素，正常情况下一粒米就是一个连通域。米粒互不接触时，这一流程没有问题。一旦两粒米挨在一起，它们在二值图中连成一块，只被计为 1 粒。米粒越密，漏计越多，见图 \ref{fig:problem}。
实测粘连率 80\% 时平均少数 38.9 粒，这条误差曲线见图 \ref{fig:curve}，也正是本次作业的难点所在。

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{problem_zoom.png}
  \caption{粘连导致漏计的局部放大。右图中橙色的一整块是一个连通域，其中实际有 2 粒米。}
  \label{fig:problem}
\end{cfigure}

于是我转向网上学习新方法，在查阅文献时注意到，这个问题在已发表的工作中也常被回避。
Kurade 等人\cite{kurade2023}的米质检测系统采用“腐蚀取标记结合注水分割”，
注水分割即 watershed 算法，常译作“分水岭算法”。
该文承认这一算法分不开互相接触的米粒，因而把这些米粒从数据集中剔除。
本文试着解决这一问题。

本文的工作可以概括为以下四点。
\begin{enumerate}
  \item 由程序从图像自身量出单粒米的尺寸与形状，后续判据均以此为单位，不预设固定的像素常数；
  \item 不预先指定颜色通道与阈值方式，而是把若干组合逐一试过，按一个打分函数自动选取；
  \item 以“又大又凹”的形状为条件，判断连通域是否为粘连块，再用注水分割切开；
  \item 切分后按面积与形状复核，并剔除硬币、边框等非米目标。
\end{enumerate}

在三组性质不同的数据上，上述方法的平均绝对误差为 2.75、0.25 与 0.50 粒，
优于本文实现的五种经典对比方法；粘连率 80\% 时误差约为直接统计连通域的四十四分之一。
在此之外，本文还用 SAM 3 的输出作伪标签蒸馏了一个 27 万参数的小网络，
详见第 \ref{sec:student} 节，在同一批测试图上它的总体误差为 0.68 粒，方法一为 0.48 粒。
代码、数据来源与复现方式见附录 \ref{app:repro}。

\section{相关工作}

分开粘连目标大致有两种思路。

一种是从轮廓入手。两粒米的接触处在轮廓上是一个凹点，找到凹点再连线切开。
Tan 等人\cite{tan2019}用这种方法处理粘连稻谷，
Avzalov 等人\cite{avzalov2025}进一步用椭圆拟合滤除假凹点。本文将后者实现为对比方法 B5。

另一种是从区域入手，先用注水分割\cite{vincent1991}切开，再修正结果。
Gamarra 等人\cite{gamarra2019}先以大小和凸性判断哪些区域需要切分，切开后再合并过碎的部分。
本文方法属于后一类，结构上与之相近；我实现该方法以后查文献，才发现有前辈用过类似的方法。

国内在谷物考种一类的任务上做过不少同类工作。凌云等人\cite{ling2005}较早把注水分割用在谷物籽粒上，
杨蜀秦等人\cite{yang2009}处理的正是连接在一起的米粒。
荀一等人\cite{xun2010}在连续腐蚀与膨胀的过程中找出两粒之间的公共区域，取其端点连线切开。
柳冠伊等人\cite{liu2014}把距离变换图当作地形图，沿山谷线切分。
他们还指出，这样一来就不必先由人工设定阈值去判断是否粘连。
这一点与本文用自标定量出判据的想法相近。
李文勇等人\cite{li2015}则先用形状因子判断一个区域是不是粘连，再定位分割点。

\section{方法一：几何方法}

方法一沿用常规的思路，先二值化，再找连通域，最后用注水分割把粘连的米粒切开。
不同之处在于，判断时用到的尺寸和门限都不预先设定，而是从每张图自身量出来。

整个流程分五步，见图 \ref{fig:flow}。第一步选出米粒所在的前景，第二步量出一粒米有多大，
第三步找出哪些连通域是几粒米粘在一起，第四步把这些粘连块切开，
第五步剔除硬币、木框、阴影这类不是米的区域，复核切分结果，并找回被去噪抹掉的米，
其中剔除的几道判断在程序里排在第三步之前。各步在一张合成图上的中间结果见图 \ref{fig:pipeline}。
下面先交代几个后文要用的概念，再按这五步依次说明。

\begin{cfigure}
  \centering
  \input{flowchart.tex}
  \caption{方法流程图。}
  \label{fig:flow}
\end{cfigure}

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{pipeline_synthetic.png}
  \caption{各步骤在一张合成图上的中间结果。}
  \label{fig:pipeline}
\end{cfigure}

\subsection{几个基本概念}

\textbf{距离变换 $D$} 是每个像素到最近背景像素的距离。
米粒中间的值大、边缘的值小，像一座山；两粒米贴合的地方是一道山谷。

\textbf{种子} 是告诉程序“这里有一粒米”的起点，只需落在米粒内部的一小块像素上。

\textbf{注水分割} 把距离变换取负值当作地形，每粒米陷成一个盆地，见图 \ref{fig:terrain}。
水从各个种子处涨起来，两股水相遇的地方就是切分线。
给几个种子就切成几块，所以一个粘连块被算作几粒，取决于种子怎么放。

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{terrain_3d.png}
  \caption{把距离变换取负值当作地形。(a) 为一个约 8 粒的粘连块，(b) 为它的三维地形，
  竖轴为距背景的像素距离，(c) 为注水后的切分结果。}
  \label{fig:terrain}
\end{cfigure}

\textbf{凸实度 $\rho$} 是区域面积与其凸包面积之比，凸包即包住该区域的最小凸多边形。
一粒米接近凸形，$\rho$ 接近 1；两粒粘在一起，接缝处凹进去，$\rho$ 会变小。

\subsection{第一步，选出米粒所在的前景}

最简单的做法是用 Otsu 法把灰度图分成亮暗两类。可是真实照片里，白米放在深色衬布上，
衬布又铺在浅色桌面上，画面有三个亮度层次。分两类只能切一刀，
无论切在哪里，总有一整片背景被划进前景，见图 \ref{fig:binfail}。
这片伪前景比所有米粒加起来还大，后面每一步都会被它带偏。

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{binarisation_failure.png}
  \caption{固定用灰度两类阈值的后果，蓝色为该二值图中最大的连通域。
  前景取亮或取暗都会多出一片巨大的伪前景。}
  \label{fig:binfail}
\end{cfigure}

所以本文不事先决定用哪个颜色通道、分几类、前景取亮还是取暗，
而是把这些组合都试一遍，用下式打分，选分数最高的一个。
\begin{equation}
  \mathrm{score} = N\sub{单} \cdot A_0 \cdot \gamma
  \label{eq:score}
\end{equation}
式中 $N\sub{单}$ 是大小接近一粒米的连通域个数，$A_0$ 是估计出的单粒面积，
$\gamma$ 是前景与紧邻背景的反差。前两项奖励“分出了许多大小一致的米粒”；
第三项用来排除噪声斑点，它们数量很多，却几乎没有反差。

选中的方案还要过一道合理性检查，米粒的长宽比和大小要在合理范围之内，
不通过就换下一个候选。低分辨率图里被误当成米粒的木框条纹，就是靠这道检查挡下的。

\subsection{第二步，量出一粒米有多大}

后面判断多大算大、多凹算凹，都需要一粒米作参照。
三组数据的米粒大小相差好几倍，固定的像素数难以通用，只能每张图现量。

做法是统计所有连通域的面积，取出现次数最多的那个大小作为单粒面积 $A_0$，
实现见代码 \ref{code:a0}。单独的米粒通常最多，粘连块的面积则落在 $A_0$ 的两倍、三倍附近。
面积先取对数，这些峰就变得等间距，更容易分辨，见图 \ref{fig:calib}。
起初取的是面积最小的那个峰，结果常常选中比米粒还小的碎屑。

有了 $A_0$，再从大小接近一粒米的连通域里量出短轴 $b_0$、凸实度 $\rho_0$ 和宽度 $w_0$，
作为后面各步的尺子。

\begin{lstlisting}[float=!htbp, caption={估计单粒面积 $A_0$}, label={code:a0}]
def estimate_a0(areas):
    """取对数面积分布的最高峰作为单粒米面积"""
    log_s = np.log(areas)
    kde = gaussian_kde(log_s)                 # 核密度估计，相当于平滑的直方图
    grid = np.linspace(log_s.min(), log_s.max(), 512)
    a0 = np.exp(grid[np.argmax(kde(grid))])   # 最高峰对应的面积
    # 再取 0.65~1.45 倍 A0 之间的连通域求平均，结果更稳
    band = areas[(areas >= 0.65 * a0) & (areas <= 1.45 * a0)]
    return band.mean() if len(band) > 0 else a0
\end{lstlisting}

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{calibration.pdf}
  \caption{面积分布的最高峰即单粒面积 $A_0$，粘连块集中在它的整数倍附近。}
  \label{fig:calib}
\end{cfigure}

\subsection{第三步，找出粘连块}
\label{sec:touch}

比一粒米大的连通域不一定是几粒粘在一起，也可能只是一粒个头大的米。
两粒米贴在一起时接缝处会凹进去，所以本文要求“又大又凹”才算粘连，实现见代码 \ref{code:touch}，
单粒和粘连块的样子可以对照图 \ref{fig:criterion}。
最早我只要求大或凹满足一条，结果真实照片上不少单粒也被切开了。

凸实度这里我还走过一次弯路。当时担心米粒只有几个像素宽时边缘锯齿太重，凸实度量不准，
就让短轴不足 4.5 像素的图改看厚度，也就是面积与最大内切圆直径平方之比。
后来拿标注框一块块对，才发现这个担心是多余的。
D1 里短轴 3.7 至 4.5 像素的那些照片，两粒粘连块的凸实度比单粒低 0.20，单粒基本不低，照样分得开；
改看厚度后，反倒有三分之二的两粒块没认出来。
到了 D3，厚度判据还把木框边那些不是米的长条当成粘连块，按面积折成好几粒，
而这些长条和单粒一样凸，按凸实度本来就能排除。
现在各种尺度都只看凸实度，改动前后的对比见第 \ref{sec:ablation} 节。

\begin{cfigure}
  \centering
  \includegraphics[width=0.7\textwidth]{touching_criterion.png}
  \caption{单粒与粘连块的对照。蓝色为区域本身，浅色为凸包多出的部分。
  橙圈为最大内切圆，它的直径即第 \ref{sec:correct} 节异物判据所用的宽度 $w$。}
  \label{fig:criterion}
\end{cfigure}

\begin{lstlisting}[float=!htbp, caption={判断一个连通域是不是粘连块}, label={code:touch}]
def is_touching(c, cal):
    """c 是一个连通域，cal 是自标定得到的单粒米参数"""
    if c.area <= 1.2 * cal.a0:            # 不比一粒米大，不算粘连
        return False
    return c.solidity < cal.rho0 - 0.06   # 又大又凹才算粘连
\end{lstlisting}

\subsection{第四步，切开粘连块}

判出粘连块之后，要决定放几个种子，也就是认定块里有几粒米。


关键在于看深度而不是距离。两粒米之间是一道深谷，一粒米内部只有浅浅的起伏。
于是对距离变换做 h-maxima，只保留比周围高出 $h$ 以上的峰作种子，
见图 \ref{fig:seeddepth} 与代码 \ref{code:split}。
$h$ 随短轴 $b_0$ 缩放，因为两粒米能靠多近取决于它们的宽度；
但不低于一个像素，因为距离变换本身就以像素计量，更小的起伏只是边缘的锯齿。

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{seed_depth.pdf}
  \caption{为什么看深度而不看距离。左为粘连块的距离变换，右为沿山脊的剖面。
  谷底低于橙色虚线，即比较低的峰矮出 $h$ 以上，两个峰各留一个种子。}
  \label{fig:seeddepth}
\end{cfigure}

这一步有个问题，我在检查效果图时才发现。h-maxima 作用在浮点的距离图上，
会把同一个平顶的峰碎成几小块，每块都成了种子，一粒米就被切成两半。
同一粒米上的碎块彼此很近，不同米粒的种子则隔得较远，
所以把相距不到约半粒米宽的种子并成一颗。

这个问题还牵连着另一个问题。碎块没合并时，$h$ 越小碎块越多，调参只好把 $h$ 调大；
$h$ 太大，又把饱满米粒之间那道凹陷部分抹平了，许多粘连块切不开。
两种错误在数量上碰巧抵消，总数看着是对的，效果图却明显不对。
合并碎块之后再把 $h$ 调小，两种错误都基本消失，量化结果见第 \ref{sec:ablation} 节。

\begin{lstlisting}[float=!htbp, caption={放种子并做注水分割}, label={code:split}]
def split_cluster(mask, cal, beta=0.1):
    dist = cv2.distanceTransform(mask, cv2.DIST_L2, 5)
    h = max(beta * cal.b0 / 2, 1.0)        # 深度门限跟着米粒宽度变，不低于 1 像素
    peaks = h_maxima(dist, h)              # 只留下足够深的山峰
    r = round(0.6 * cal.b0 / 2)            # 相距不到 0.6 倍短轴的峰算同一粒米
    seeds = label(cv2.dilate(peaks, disk(r))) * peaks
    if seeds.max() <= 1:                   # 只有一个种子，不用切
        return (mask > 0).astype(int)
    return watershed(-dist, seeds, mask=mask > 0)
\end{lstlisting}

\subsection{第五步，剔除非米区域、复核切分、找回漏掉的米}
\label{sec:correct}

这一步里的几件事，在程序中并不都排在切分之后，执行顺序以代码 \ref{code:count} 和图 \ref{fig:flow} 为准。
每个连通域先要过碎屑、异物、暗区这三关，剩下的才交给第三步判断是否粘连；
粘连块切开后还要复核；等所有连通域都数完，最后再回开运算之前的阈值图里找回漏掉的米。
放在一节里讲，是因为它们都在回答“这一块到底算几粒米”。
一个粘连块从切开到定出粒数的过程见图 \ref{fig:cluster}。

面积不到 0.45 倍单粒的区域当作碎屑，直接忽略。
这个门限原来是 0.3，粘连判据改用凸实度以后切开的块变多，切口处 0.3 至 0.45 倍大的碎片开始被计数，我才把它调高。

硬币、木框这类异物面积很大，和米堆有两点不同。
米粒只是挨着，不会融成一体，米堆最厚的地方也只有一粒米那么厚，硬币和木框则厚得多。
厚度用区域内能放下的最大圆的直径来量，记为宽度 $w$，单粒的宽度记为 $w_0$，即图 \ref{fig:criterion} 中的橙圈。
另外，几粒米粘在一起通常会留下凹口，面积数倍于单粒、却和单粒一样凸的区域，多半不是米堆。
这条判据最初还要求异物比米粒更圆，细长的木框条因此漏了过去，去掉这一条后才拦住。

还有一类异物大小形状都和米粒差不多，上面的办法拦不住，
比如木框、墙面边缘的反光亮条，以及硬币浮雕被二值化后碎成的小块。
它们露馅在周围。米粒躺在深色的底上，四周都是底，这些东西至少有一边挨着亮的木头或硬币面。
于是我在区域外围取一圈，从外沿 2 像素一直到约两粒米宽，数其中有多少背景像素是亮的，超过两成就判为异物。
“亮”要同时满足两条，比 Otsu 在背景上分出的阈值亮，也比底色和米粒亮度的中点亮。
只用前一条，整张黑底的图会被 Otsu 从中间劈成两半；只用后一条，D3 的米粒偏暗，中点也跟着偏低。

暗区是后来才发现的。不少图选中的二值化方案是“饱和度低的算前景”，可纯黑和白米的饱和度一样低，
木框边的黑色阴影条和一些暗斑就这样混进了前景，大小形状又都像米。
一看灰度就清楚了，它们只有米粒的百分之几，米粒本身则在米粒中位灰度的 0.9 至 1.1 倍之间。
所以取所有候选米粒灰度的中位数作参照，平均灰度不到它一半的区域都当作异物。

粘连块切开以后，面积不到 0.4 倍单粒的碎块多半是切多了，并回与它接壤最多的那一块。
如果某一块切完后面积仍超过 1.5 倍单粒，凸实度又比单粒低 0.03 以上，就说明它没切开。
这时先按面积算出它该有几粒，再取距离图上最高的那几个峰作种子重切一次，
切出的每一块大小都像一粒米才采用，否则只按面积计数。
起初这里只按面积计数，粒数大多是对的，但效果图上几粒米会被画成一整块。

最后是找回。拿标注框查漏掉的米时我发现，真正没被阈值分出来的只是少数，
大多数在阈值图里原本是完整的一粒，是第一步去噪用的 $3\times3$ 开运算把它们削掉了。
D3 的米只有三四像素宽，开一次就能削掉大半，甚至整粒抹掉，剩下的部分又被碎屑门限丢掉。
干脆去掉开运算也不行，噪点会一起涌进来，D3 的误差反而升到 5 粒以上。
所以我在最后补了一步，回到开运算之前的阈值图，找那些既没被计数、也没被判为异物的区域，
只要面积在单粒的 0.3 至 1.3 倍之间，短轴不小于单粒短轴的一半，长宽比不超过单粒的三倍，
亮度不低于米粒的一半，又不挨着亮背景，就补记一粒。
这里的单粒面积要在开运算之前的阈值图上重新量，否则会被开运算缩小后的尺寸带偏。

上面这些门限，碎屑的 0.45、亮背景的两成、暗区的一半，以及找回时面积和短轴的两个下限，
都只在 D1 上挑选，D3 没有参与，在 D3 上也同样有效，详见第 \ref{sec:ablation} 节。

\begin{lstlisting}[float=!htbp, caption={计数主循环}, label={code:count}]
total = 0
for c in components:
    if c.area < 0.45 * a0:                 # 碎屑，忽略
        continue
    if is_foreign(c, cal) or near_bright(c, cal, bright) or is_dark(c, gray, ref):
        continue                           # 硬币、木框、亮边、阴影等，不计数
    if not is_touching(c, cal):            # 单独的一粒
        total += 1
        continue
    parts = merge_small(split_cluster(c.mask, cal), a0)  # 小于 0.4*A0 的碎块并入邻居
    for p in regionprops(parts):
        k = p.area / a0
        if k < 0.4:                        # 没有邻居可并的碎块
            continue
        if k > 1.5 and p.solidity < cal.rho0 - 0.03:
            n = round(k)                   # 没切开，按面积它该有 n 粒
            pieces = split_by_count(p.mask, n, cal)   # 取最高的 n 个峰重切一次
            if pieces_look_like_grains(pieces, a0):
                parts_drawn.extend(pieces)            # 切对了就按切开的画
            total += n                     # 粒数都按 n 计
        else:
            total += 1
total += len(recover_erased(raw_mask, counted, ref))  # 找回被开运算抹掉的米
\end{lstlisting}

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{cluster_detail.png}
  \caption{粘连块的处理过程，自左至右为粘连块、距离变换、种子与最终结果。}
  \label{fig:cluster}
\end{cfigure}

\section{方法一的实验与分析}

这一节先拿方法一和五种常见做法比准确率，再看粘连加重、图像变差时它能撑到什么程度，
然后逐项拆掉各步，看每一步起多大作用，最后和视觉大模型 SAM 3 比准确率，也比算力。

\subsection{数据集}

D1、D3 两组数据以及 D2 所用的米粒素材，都取自 Roboflow Universe 上的公开数据集，许可为 CC BY 4.0。
第 \ref{sec:student} 节的方法二沿用这三组数据。

\textbf{D1，真实照片，51 张。} 取自 Roboflow 上的 rice 数据集\cite{ds_d1}，
手机拍摄，深色布上摊开的米粒，每张 71--124 粒，带一枚参照硬币，每粒米都有人工标注框。
米粒基本不粘连。

\textbf{D2，自制合成图，40 张。} 从 RICE 数据集\cite{ds_grains}中 Karacadag 品种的单粒米照片里
抠出米粒，随机旋转、缩放后贴到深色画布上，
粘连率分 0\%、20\%、40\%、60\%、80\% 五档，每档 8 张，每张 44--98 粒。
它的真值由构造方式精确给定，粘连程度也可控，专门用来考粘连。

\textbf{D3，低分辨率，719 张。} 取自 Roboflow 上的 RICE 数据集\cite{ds_d3}，
$224\times224$ 的小图，每张 4 至 21 粒，平均约 15 粒。该数据集导出时做过翻转增强，同一源图存在多个副本，
按源图名去重后保留 719 张。

\subsection{评价指标}

记第 $k$ 张图的真值为 $g_k$、预测为 $p_k$。
主要指标为平均绝对误差 $\mathrm{MAE}=\frac1n\sum_k|p_k-g_k|$，即平均每张数错几粒。
另外给出准确率 $1-\mathrm{MAPE}$ 与最差单张误差，
其中 MAPE 为平均相对误差 $\frac1n\sum_k|p_k-g_k|/g_k$。
平均值小并不排除个别图像错得离谱，所以两者一并看。

\subsection{与其他方法比较}

先看方法一比常见做法准多少。
为使比较只落在“如何分开粘连”这一步上，所有方法都跑在同一份二值图上。
B1 直接统计连通域个数，不对粘连作任何处理。
B2 用前景总面积除以单粒面积。
B3 是距离变换注水分割，思路来自 OpenCV 的教程，种子取 $D > 0.5\max D$，教程原文取 0.7 倍。
B4 腐蚀后取连通域作种子，即文献\cite{kurade2023}的做法。
B5 为轮廓凹点 + 椭圆拟合\cite{avzalov2025}。

结果见表 \ref{tab:comparison}。三组数据上，方法一的平均误差都是六种几何方法中最低的。
三种对比方法的失效值得单独说一下。

\begin{ctable}
  \centering
  \caption{三组数据上的总体结果，误差以粒计，“最差”一列为最差单张误差。
  加粗为六种几何方法中每列最好的值，SAM 3 另列在最后作参照。
  B5 是在每个数据集上分别调到最好后的结果。}
  \label{tab:comparison}
  \tabfit{\input{tables/comparison.tex}}
\end{ctable}

B4 在合成数据上除不粘连的一档外，误差与 B1 完全相同。一次 $3\times3$ 腐蚀不足以分开粘连块，
每块仍只产生一个种子，注水分割什么也没切开，等于退化成连通域计数。

B3 在真实照片上误差达 74.90 粒。它的阈值取自整张图距离变换的最大值，
而画面里的硬币远大于米粒，把阈值整体抬高，大多数米粒的种子被抹掉了。
图 \ref{fig:b3seeds} 是同一张照片上两种取种子方式的对照，
B3 全图只留下 19 个种子，方法一按连通域分别标定，得到 148 个。

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{b3_seeds.png}
  \caption{B3 的全局阈值与本文自适应种子的对照，红色为种子，灰色为二值图。
  本文对每个连通域分别放种子，粘连块会得到不止一个，因此总数多于米粒数。}
  \label{fig:b3seeds}
\end{cfigure}

B5 难以同时兼顾两种尺度。它在轮廓上作一个小圆找凹点，米粒只有 4 像素宽时，
圆几乎盖住整粒米，孤立的单粒也会被判出凹点。把圆调大，它在 D2 上又几乎不再切分。
需要说明，原文的椭圆拟合为穷举搜索，本文实现的是贪心近似，对它未必公平。

这三种方法失效的原因相同，都用了预先设定的固定数值。
换一种米粒尺度，这些数字就不再合适，方法一的判据从图像自身量出，所以在三组数据上表现都比较稳。

\subsection{误差随粘连程度的变化}

再看粘连加重时方法一还准不准。结果见表 \ref{tab:touching} 与图 \ref{fig:curve}。
粘连率自 0\% 升至 80\%，连通域计数的误差由 0.25 粒一路升到 38.88 粒，
方法一在五档上均未超过 0.9 粒。
图 \ref{fig:scatter} 是逐张的预测值与真值对照，格子集中在对角线附近，没有整体偏多或偏少。

\begin{ctable}
  \centering
  \caption{合成数据上按粘连率分层的平均绝对误差，单位为粒，加粗为每一档误差最小的方法。
  B5 用的是默认设置，即圆半径取 0.5 倍短轴并做椭圆校正，与表 \ref{tab:comparison} 中按数据集调到最好的 B5 不同。}
  \label{tab:touching}
  \tabfit{\input{tables/touching.tex}}
\end{ctable}

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{error_vs_touching.pdf}
  \caption{计数误差随粘连率的变化。}
  \label{fig:curve}
\end{cfigure}

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{pred_vs_true.pdf}
  \caption{预测值与真值对照的热力图。格子颜色表示落在该格的图片张数，虚线表示两者相等。}
  \label{fig:scatter}
\end{cfigure}

\subsection{图像质量变差时的表现}
\label{sec:robust}

为了找出方法一在哪里失效，我在 D2 上人为加了模糊、噪声、对比度压缩和降采样四类退化，
结果见表 \ref{tab:robust}，模糊强度以单粒宽度 $b_0$ 为单位。

噪声加到 45 个灰度级、对比度压到 8\%，误差基本没变。
降采样到 1/6 时米粒只剩约 4 像素宽，中位误差为 2 粒，误差是一点点涨上去的，没有突然崩掉。
模糊就不一样了。在 0.20 至 0.35 倍 $b_0$ 的轻度模糊下，中位误差还在 2 粒以内，平均误差却猛涨，
因为有 2 张图模糊后整个选错了二值化方案，一张就数出上千粒。
模糊到 0.70 倍 $b_0$，中位误差也升到 15 粒，40 张里崩了 13 张。方法一真正怕的是模糊。

\begin{ctable}
  \centering
  \caption{合成粘连图 D2 在不同退化下的计数误差，共 40 张。“崩溃”指单张误差超过 100 粒，
  出现崩溃的格子加粗。}
  \label{tab:robust}
  \tabfit{\input{tables/robustness.tex}}
\end{ctable}

\subsection{消融实验}
\label{sec:ablation}

这一小节把方法一的各步逐项关掉，或改回修正前的写法，看误差怎么变，结果见表 \ref{tab:ablation}。

\begin{ctable}
  \centering
  \caption{去掉或改动某一步后的平均绝对误差，单位为粒，第一行为完整方法。
  每列加粗的是误差最大的一项，即去掉后损失最大的那一步。}
  \label{tab:ablation}
  \tabfit{\input{tables/ablation.tex}}
\end{ctable}

先看切分本身。种子碎块如果不合并，一粒米常被切成两半，D2 的误差从 0.25 粒涨到 3.17 粒。
修正之前我正是这样做的，只好把 $\beta$ 抬到 0.45 来压住碎块，代价是许多粘连块切不开，D2 仍有 1.77 粒。
按面积补数的形状先验校正，兜住的是注水分割没切开的那些块，去掉它，D2 升到 1.12 粒，D1 从 2.75 粒升到 4.45 粒。

几道剔除和找回的判据各有分工。异物剔除主要对付 D3 的木框，去掉后 D3 从 0.50 粒升到 2.00 粒。
去掉暗区判据，D1 和 D3 分别升到 3.53 粒和 0.90 粒；不找回被开运算抹掉的米，则分别升到 4.22 粒和 0.74 粒。
亮背景判据只在 D1 上有用，去掉后 D1 升到 3.25 粒。D3 上木框边的东西大多是阴影，
已经被暗区判据挡掉了，去掉亮背景判据反而略好一点，为 0.45 粒。

表里还有两项是改回修正前的写法。细米粒改回厚度判据，D1 升到 4.51 粒，D3 却降到 0.38 粒；
碎屑门限改回 0.3，D1 几乎不变，D3 升到 0.66 粒。

尺度自标定对付的是真实照片里参差的米粒大小。整个数据集共用一个 $A_0$ 时，D1 从 2.75 粒升到 6.31 粒。
D2 倒是从 0.25 粒降到 0.17 粒，合成图的米粒本来就大小整齐，一个固定值比每张现量更稳。
只用灰度通道时 D1 更好，为 2.33 粒，但 D3 从 0.50 粒升到 1.00 粒，饱和度通道还不能去掉。

有好几项在 D3 上反而更好，除了上面提到的亮背景判据和厚度判据，
去掉形状先验校正和去掉尺度自标定也是，D3 分别为 0.42 粒和 0.47 粒，但它们都让 D1 明显变差。
我的取舍只看 D1，D3 留作检验；要是回头按 D3 改设置，D3 就不再是没参与挑选的数据了，所以都没有改。
另外，粘连判据改成“或”后 D2 略降，种子碎块不合并且 $\beta$ 取 0.45 时 D1 略降，也都以别的数据变差为代价。

\subsection{与 SAM 3 对比}
\label{sec:sam3}

最后把方法一和视觉大模型 SAM 3\cite{sam3} 放在一起比。
SAM 3 是去年冬天由 Meta 推出的视觉分割模型，我在做大创项目的时候经常接触，它可以由文本提示驱动，
给出“rice grain”之类的提示，就能返回每粒米的掩膜，无需训练。
本文先在 D1 上比较了不同提示词与阈值，结果见表 \ref{tab:sam3}，
再把各提示词在三组数据上分别调到最好，结果见表 \ref{tab:sam3matrix}。

\begin{ctable}
  \centering
  \caption{SAM 3 在真实照片 D1 上使用不同提示词和阈值的平均绝对误差，单位为粒，加粗为这三个提示词中误差最小的一格，提示词 white seed 的结果见表 \ref{tab:sam3matrix}。}
  \label{tab:sam3}
  \tabfit{\input{tables/sam3.tex}}
\end{ctable}

\begin{ctable}
  \centering
  \caption{各提示词在各数据集上分别调到最好时的误差，$\tau^\ast$ 为对应阈值，
  加粗为每组数据上误差最小的提示词。}
  \label{tab:sam3matrix}
  \tabfit{\input{tables/sam3_matrix.tex}}
\end{ctable}

即便提示词选得恰当，SAM 3 也只在 D3 上比方法一准。
\texttt{white seed} 在三组数据上分别为 3.29、0.65、0.07 粒，方法一为 2.75、0.25、0.50 粒。
D1 和重度粘连的 D2 上都是方法一更准。
但它对提示词相当敏感，五个提示词中有三个在合成图上一粒也没检出，
其中包括看起来最自然的 \texttt{rice grain}，而模型不会提示自己失败。

图 \ref{fig:render_d1} 至图 \ref{fig:render_d3} 是每组数据一个场景的逐粒结果，便于核对。
每粒米涂一种颜色，相邻两粒颜色不同。
虚线框与 $\times N$ 标出的区域并未被真正切开，其粒数由面积推断而来。
灰色并标 $\times$ 者为判定的异物，不计入总数。

\begin{cfigure}
  \includegraphics[width=\textwidth]{render_d1.png}
  \caption{真实照片 D1 上的结果，左为本文方法、右为 SAM 3。}
  \label{fig:render_d1}
\end{cfigure}

\begin{cfigure}
  \includegraphics[width=\textwidth]{render_d2.png}
  \caption{合成粘连图 D2 中重度粘连场景的结果，左为本文方法、右为 SAM 3。}
  \label{fig:render_d2}
\end{cfigure}

\begin{cfigure}
  \includegraphics[width=\textwidth]{render_d3.png}
  \caption{低分辨率图 D3 上的结果，左为本文方法、右为 SAM 3。}
  \label{fig:render_d3}
\end{cfigure}

D1 这张方法一数出 98 粒，和真值一样，按标注框一粒粒对下来，只漏了 1 粒、多了 1 粒。
左上方硬币的浮雕被二值化成几块碎片，四周都是亮的硬币面，都被判成了异物，图中用灰色标出。
改粘连和异物判据之前，这些碎片曾被数成 8 粒，和别处漏掉的 7 粒一抵，总数才只多 1 粒。
SAM 3 同样没把硬币算进去，但只数出 93 粒。
D2 这张方法一数出 63 粒，真值 64 粒，每个粘连块都是真正切开的，没有靠面积折算。
这一张 SAM 3 恰好全对，不过放到全部 40 张上，调好的 SAM 3 平均误差 0.65 粒，仍不如方法一的 0.25 粒。

\subsection{算力成本对比}
\label{sec:cost}

准确度之外，两种方法的算力开销差别很大。
测量在同一台笔记本上进行，配置为 Intel i9-13900H 处理器与 12 GB 显存的 RTX 4080 Laptop GPU。
每组数据取 10 张图，测每张的处理时间，不含读图，并记录峰值内存和显存，结果见表 \ref{tab:cost}。
SAM 3 在 CPU 上太慢，每组只测了 1 张。CPU 上用 32 位浮点，GPU 上用 16 位浮点 bfloat16。


\begin{ctable}
  \centering
  \caption{单张图的处理时间和资源占用，显存为分配峰值，SAM 3 在 CPU 上每组只测 1 张。
  每行加粗的是最省的一项。}
  \label{tab:cost}
  \tabfit{\input{tables/cost.tex}}
\end{ctable}

有显卡时，SAM 3 每张约 0.21 s，方法一的 GPU 版在 D1 上和它相当，在 D2 上却要 1.25 s。
D2 的图大、米粒也大，亮背景判据要在每个连通域外围取一圈约两粒米宽的带子，
找回步骤又要把开运算前的阈值图重新过一遍，这两步都在 CPU 上做，占了 D2 耗时的大半。
但 SAM 3 先要加载 8.4 亿参数、3.2 GB 的权重，运行时占 5.6 GB 内存和 2.5 GB 显存；
方法一没有要学习的参数，GPU 版只占 0.10 GB 显存。
没有显卡时差距就大了，SAM 3 每张 17 s 以上，方法一在 CPU 上为 0.04 至 1.44 s。

\subsection{本节小结}

方法一在三组数据上都比五种常见做法准，粘连加重时误差几乎不涨，在真实照片上甚至比 SAM 3 还准。
它也有几处毛病。怕模糊；在低分辨率的 D3 上仍不如 SAM 3，为 0.50 粒对 0.07 粒；
二值化方案一旦选错还会整张失败，D3 里有 8 张图因此只数出 0 至 4 粒。
SAM 3 在低分辨率图上更准，却要挑提示词，也离不开显卡。
我想试试能不能兼顾两边，这就有了第二种方法。

\section{方法二：SAM 3 作为教师模型训练专用小模型}
\label{sec:student}

\subsection{方法二思路的由来}

第二种方法是在方法一还很粗糙的时候想到的。那时方法一在 D3 上误差 1.90 粒，
零样本的 SAM 3 用提示词 grain、阈值 0.35，只有 0.18 粒。
方法一错的地方大多是两类，木框边的东西被当成米，颜色偏暗的米被漏掉。
后来一粒粒核对才知道，前者多半是黑色阴影，后者多半是被开运算抹掉的米，
补上第 \ref{sec:correct} 节的几条判据后，方法一在 D3 上降到了 0.50 粒。

可还有一类错是几何判据补不上的。方法一得先挑一个二值化方案，挑错了整张图就全错。
几何判据只知道米长什么样，并不知道什么是米。SAM 3 知道，那能不能把它知道的东西教给一个小模型？

\subsection{具体实现方式}

\textbf{制作标注。}用 SAM 3 处理每张照片，取其每个掩膜的质心，得到一串点坐标作为伪标签。
合成图 D2 则直接采用生成时记录的米粒中心，不必经过教师。
教师取提示词 white seed、阈值 0.40。拿它与人工标注逐点匹配，教师给出的点在 D3 上准确率 0.993、召回率 1.000、计数误差 0.10 粒，
在 D1 上依次为 0.957、0.967 与 3.29 粒。逐点看，这两组数据上的伪标签足够干净，
但 D1 上的计数误差并不比方法一小。
人工标注全程不参与训练，只用于测试。
图 \ref{fig:teacherlabel} 是其中一张照片的逐点对照，漏掉的和多给的都是少数。

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{teacher_labels.png}
  \caption{SAM 3 给出的点与人工标注的对照。绿圈为两者配对上的米粒，
  橙圈为 SAM 3 漏掉的，蓝方块为它在同一粒米上多给的点。}
  \label{fig:teacherlabel}
\end{cfigure}

\textbf{学什么。}学生模型学的是密度图，整张图积分就是粒数。
这样做是因为米粒只有 4 像素宽，要网络画出清晰的实例边界不现实，让它在米粒处“鼓起一个包”却做得到；
损失函数也不要求把相邻两粒分开，而那恰恰是几何方法最吃力的地方。
高斯的宽度按每张图自身的米粒间距确定，因此 4 像素与 25 像素的米粒在网络看来尺度相当。
从教师的掩膜到学生的预测，整条流程见图 \ref{fig:studentpipe}。

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{student_pipeline.png}
  \caption{方法二的流程。}
  \label{fig:studentpipe}
\end{cfigure}

\textbf{网络与训练。}三层 U-Net，27 万参数，权重文件 1 MB。
按 7:1:2 划分训练、验证与测试集，划分以源图为单位。在 RTX 4080 上训练 6000 步，约 6 分钟。

\begin{ctable}
  \centering
  \caption{学生模型与本文方法、教师在同一批测试图上的平均绝对误差，单位为粒。
  教师即提示词 white seed、阈值 0.40 的 SAM 3；表中只统计这 162 张测试图，所以与前面各表中全部数据上的数字不同。}
  \label{tab:student}
  \tabfit{\input{tables/student.tex}}
\end{ctable}

\subsection{结果}

结果见表 \ref{tab:student}，表中还列了不作任何粘连处理、直接数连通域的结果 4.46 粒，四者用的是同一批图。
学生模型的总体误差为 0.68 粒，不如方法一的 0.48 粒。
在训练数据最多的 D3 上两者差不多，学生模型 0.42 粒，方法一 0.38 粒；
D1 和 D2 上方法一明显更好，学生模型分别为 3.91 粒和 1.29 粒，方法一为 2.00 粒和 0.25 粒。
它也不及教师的 0.25 粒，这在意料之中，蒸馏一般追不上教师。
好处是体量小，只有 27 万参数、1 MB 权重，约为 SAM 3 的三千分之一，
每张图耗时 2 至 25 ms，比 SAM 3 快 8 到一百多倍，见表 \ref{tab:cost}。
也不用再挑提示词。SAM 3 换个提示词就可能一粒也检不出，学生模型则把选好的那套行为固定了下来。

学生模型真正的长处是不会整张失败。图 \ref{fig:studentwin} 是 D3 上的一个例子。
方法一挑二值化方案时选错了，前景选成了两侧的木框，木框又被判为异物，全图一粒没数，真值是 16 粒。
学生模型没有“挑二值化方案”这一步，密度图上 16 粒米各鼓起一个包，积分得 16.5 粒。
它并没有多用什么形状判据，只是从教师那里学到了米粒该长什么样。

\begin{cfigure}
  \centering
  \includegraphics[width=\textwidth]{student_vs_ours.png}
  \caption{同一张低分辨率图上两种方法的对照。中图里方法一把两侧木框选成了前景，
  灰色标出的木框被判为异物，一粒米也没有数到；右图为学生模型预测的密度图。}
  \label{fig:studentwin}
\end{cfigure}


\section{不足}
\label{sec:limit}



\textbf{画面里有比米更整齐的东西时，会把它当成米。}
我另找过一组带直尺的照片\cite{ds_ruler}，想补充粘连数据。
结果在不少图上，程序选出的前景是直尺的刻度，而不是米粒，见图 \ref{fig:scope}。
原因在于式 \eqref{eq:score} 挑二值化方案时，看的是画面里有多少块大小一致的小东西。
刻度线大小整齐、对比强烈，比颜色浅、大小不一的米粒更符合这个标准。
换句话说，方法一只认形状，不认得什么是米。
这组照片里还有不少是几个品种的米放在一起对比，粒径相差很大，
不符合“同一张图里米粒大小相近”这一前提，所以最终没有用作测试集。

\begin{cfigure}
  \includegraphics[width=\textwidth]{scope_failure.png}
  \caption{方法被直尺刻度误导的两个例子，每例左为原图、右为选出的前景。}
  \label{fig:scope}
\end{cfigure}

\textbf{图像模糊时，可能选错二值化方案。}
第 \ref{sec:robust} 节里出现崩溃的几张图，都是模糊造成的。
模糊之后，饱和度通道被阈值切成大量小碎点。
碎点数目多，式 \eqref{eq:score} 的得分反而略高于正确的方案，程序便选错了，一张图数出上千粒。
这个问题目前还没有解决。

\textbf{二值化选错时会整张失败。}
二值化方案按整张图打分，个别低分辨率图上选出的前景干脆是木框而不是米粒，
木框又被判为异物，一张图数出 0 粒，见图 \ref{fig:studentwin}。
这需要改二值化的候选或打分方式，目前还没有做。

\textbf{几个门限是在测试数据上挑的。}
第 \ref{sec:correct} 节里碎屑、亮背景、暗区和找回的门限，都是我在 D1 上一个个试出来的。
D3 没参与挑选，误差从 1.90 粒降到 0.50 粒，可以算作留出集上的结果。
这些门限是分两轮加进去的，每一轮我也反过来只用 D3 挑过一次，这时 D1 的误差分别是 3.57 粒和 2.88 粒，
比用 D1 挑时的 3.24 粒和 2.71 粒高出一成上下。门限对挑选用的数据还是有些迁就，换一批照片未必有这么准。





\section{结论}

本文实现了一种基于注水分割的粘连米粒计数方法。它先从图像本身量出单粒米的尺度，
据此判断粘连、控制种子数量，再按面积与形状复核切分结果，剔除硬币、木框边的亮条和阴影，
最后从去噪前的阈值图里找回被抹掉的细小米粒。
三组数据上的平均绝对误差分别为 2.75、0.25 与 0.50 粒，优于五种对比方法。

做完这份作业，体会最深的一点是，这个问题没有想象中这么简单，我看到问题的第一眼，认为只要对二值化图像稍加处理即可。但遇到了挫折，于是我去网上搜集论文，看看有没有好的方法可以参考。受到了分水岭算法的启发，针对这个问题具体化，于是有了方法一。对于方法二，是一种偷懒的黑箱做法，平均误差反倒不及方法一，但不会因二值化选错而整张失败，可解释性则弱。我做大创项目时用过 SAM 3 分割模型，于是有了蒸馏学生模型的想法。

我将在文末附上更多的效果图和参考文献，头一次用 \LaTeX{} 写报告，不太熟练，文中若有不恰当之处，还请老师批评指正。
